[project]
name = "shopify-vercel-chk"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "aiohttp>=3.9.0",
    "curl-cffi>=0.7.0",
    "flask>=3.0.0",
]

[tool.vercel]
entrypoint = "api.chk:app"
